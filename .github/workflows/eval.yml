name: Quality Evaluation

on:
  workflow_dispatch:
    inputs:
      pipeline_version:
        description: 'Pipeline version to evaluate (e.g. v1-a3b2c1d4)'
        required: false
        default: 'latest'

jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: cd engine && uv sync
      - run: echo "TODO - run evals/run_eval.py with pipeline=${{ inputs.pipeline_version }}"
